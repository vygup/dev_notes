Имя и почту пользователя (для отображения в коммитах:
```bash
git config --global user.name "name"

git config --global user.email email_user
```

Отключение проверки сертификатов ssl:
```bash
git config --global http.sslVerify false
```

